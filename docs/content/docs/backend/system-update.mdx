---
title: 部署更新与安全回退
description: 容器镜像更新、数据库备份迁移、健康验证与异常回退原则。
---

# 部署更新与安全回退

公开仓库提供版本化容器镜像与 `docker-compose.deploy.yml`，不附带宿主机自动更新守护程序。部署管理员应在宿主机完成数据库备份、镜像拉取、服务切换和健康验证；业务后端不挂载 Docker Socket。桌面应用的签名更新源和发布流程见 [桌面自动更新发布](/docs/backend/desktop-updater)。

## 安全更新顺序

1. 从 GitHub Releases 读取包含预览版在内的最高语义版本，并与 `.env` 中固定的 `CANVAS_IMAGE_TAG` 比较。`sha-*` 这类非正式镜像标签一律视为低于正式 Release，避免把 `vsha-5509b17` 字典序比较成比 `v1.5.0` 更新。
2. 下载目标版本的 `docker-compose.deploy.yml`，执行 `docker compose config --quiet`，检查部署文件、磁盘空间和当前版本。当前标签不是 `vX.Y.Z` 时，健康检查只确认服务就绪，不要求构建号和镜像标签逐字相同。
3. 创建 `backup-YYYYMMDD-HHMMSS.zip.partial`，其中包含 PostgreSQL custom-format dump、后端数据目录 tar 和元数据；写入完成后 `fsync`、原子改名，并复核 ZIP 结构和 SHA-256。
4. 拉取目标 backend/web 镜像并要求存在仓库 `sha256` 摘要。任何失败都在停服前中止。
5. 停止 web/backend，等待 Backend 的 SIGTERM drain 完成；PostgreSQL 和 Redis 保持运行。
6. 原子更新 `CANVAS_IMAGE_TAG` 和 Compose 文件，启动目标版本；Backend 会在接收请求前完成结构迁移。
7. 连续检查 `/api/health/ready` 的数据库、运行时、schema 和 build version，并经过稳定窗口后才标记成功。

## 自动回退

停服后的迁移、启动或健康验证失败会触发自动回退：

1. 停止目标 web/backend；
2. 恢复更新前保存的 Compose 文件；
3. 再次校验备份 ZIP 和 SHA-256；
4. 使用 `pg_restore --clean --if-exists` 恢复 PostgreSQL；
5. 恢复旧 `CANVAS_IMAGE_TAG`，启动旧服务并重新进行稳定窗口验证。

回退任一步失败时状态变为 `manual_intervention`，不伪造成功。管理后台仅在存在旧版本和已校验备份时开放人工回退按钮。

## 安装与升级约束

正式部署应把 `CANVAS_IMAGE_TAG` 固定到具体 Release，不使用 `latest`。更新前先备份，再拉取并重建服务：

```bash
cd /opt/beeftv
sudo docker compose --env-file .env -f docker-compose.deploy.yml pull
sudo docker compose --env-file .env -f docker-compose.deploy.yml up -d --force-recreate backend web --wait
```

确认健康检查通过后再清理旧镜像。若失败，恢复原 Compose、原镜像标签和匹配的数据库备份。

## 运维检查

```bash
sudo docker compose --env-file .env -f docker-compose.deploy.yml ps
sudo docker compose --env-file .env -f docker-compose.deploy.yml logs --tail=200 backend web
sudo ls -lh /opt/beeftv/backups
sudo sha256sum /opt/beeftv/backups/*.zip
```

备份文件包含业务数据，应限制为 root 访问并纳入异机备份策略。数据卷本身不是备份。
